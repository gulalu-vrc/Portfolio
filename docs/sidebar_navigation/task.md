# サイドバーナビゲーション実装タスク

- `[x]` 1. **タスクの作成**（本タスクリストの作成）
- `[x]` 2. **`index.html` の構造変更**
  - アプリ全体を囲むラッパーコンテナを作成
  - 左サイドバー（`<aside class="sidebar">`）の追加と7つの作品へのアンカーリンクの実装
  - 既存コンテンツを `<main class="main-content">` に移動
- `[x]` 3. **`style.css` のスタイル調整**
  - サイドバーの固定スタイル（グラスモーフィズム、色、サイズ）の定義
  - メインコンテンツの `margin-left` で被り回避
  - フルスクリーン要素（`100vw`）を `100%`幅に修正してレイアウト最適化
  - サイドバーリンクの `.active` ハイライトスタイルの追加
- `[x]` 4. **`script.js` にScrollSpyロジックを追加**
  - IntersectionObserver で各セクションの表示状態を監視
  - スクロール状況に応じてサイドバーリンクの `.active` を自動切替
- `[x]` 5. **動作確認・微調整**
  - ジャンプとスクロール追従チェック
  - Docsへのファイルの同期（`walkthrough.md`, `task.md`）
