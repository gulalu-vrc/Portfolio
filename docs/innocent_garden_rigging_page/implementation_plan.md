# イノセント・ガーデンドレス セクションへの新ページ追加計画

「イノセント・ガーデンドレス」のプロジェクト詳細に、リギングと揺れ物、および貫通対策に関する詳細ページを新たに追加します。

## ユーザーレビューが必要な事項
> [!IMPORTANT]
> - 追加する説明文の内容（現在はテクニカルアーティストとしての視点で仮の文章を配置します）。
> - 5ページ目となる「最終調整と完成」ページの内容は維持し、順序のみを変更します。

## 変更内容

### 1. ポートフォリオ ページ構成の更新
`index.html` 内の `work-innocent-garden` セクションを修正します。

#### [MODIFY] [index.html](file:///g:/Git/Portfolio/index.html)
- 現在の4枚目（最終調整と完成）の前に、新しい `work-detail-page` を挿入します。
- 新しい4枚目のレイアウト：
  - **左側 (リギングと揺れ物)**:
    - 2x2のグリッドレイアウトを採用。
    - 左上：解説テキスト。
    - 右上：`BoneFront.png`（ラベル：ボーン構造）。
    - 左下：`YureSide.png`（ラベル：揺れ設定 側面）。
    - 右下：`YureFront.png`（ラベル：揺れ設定 前面）。
  - **右側 (貫通対策)**:
    - 2列グリッドの変則レイアウト（上2枚、下1枚）。
    - 上段左：解説テキスト。
    - 上段右：`Collider.png`（ラベル：コライダー設定）。
    - 下段中央（全幅）：`Constraint.png`（ラベル：コンストレイント設定）。
- 既存の「最終調整と完成」ページを5枚目として扱います。

## 検証計画

### 手動確認
- ブラウザで `index.html` を開き、新しく追加された4ページ目が正しく表示されているか確認します。
- レイアウトが崩れていないか、画像とラベルが正しく配置されているか確認します。
- サイドバーからのナビゲーションに影響がないか確認します（ページが増えてもスクロールで辿り着けるため基本問題なし）。
