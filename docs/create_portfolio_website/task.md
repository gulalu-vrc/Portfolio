# タスクリスト (Vanilla HTML/CSS/JS 移行)

- [x] 1. プロジェクトのクリーンアップ
    - [x] Vite関連のファイル群 (`node_modules`, `src`, `package.json` 等) の削除
- [x] 2. プレーン構成の作成
    - [x] 骨組みとなる `index.html` の作成
    - [x] スタイルを定義する `style.css` の作成
    - [x] アニメーション制御用の `script.js` の作成
- [x] 3. デザイン・アニメーションの復元
    - [x] `index.html` にコンポーネントと同等の静的要素を記述
    - [x] `style.css` にダークテーマとグラスモーフィズムを記述
    - [x] `script.js` に `Intersection Observer API` 等を用いてスムーススクロールとフェードインアニメーションを記述
- [x] 4. 最終確認
    - [x] ローカルファイルとしてブラウザで直接開き、問題なく描画・動作するかを確認
