# 実装計画書: Keen ページへのギャラリータイトルと解説文追加

Keen プロジェクトの「運用を考えたシェーダー設計」ページ（3枚目）の右カラムレイアウトを調整し、情報の整理と補足説明の追加を行います。

## ユーザーレビューが必要な項目

> [!IMPORTANT]
> - タイトルのスタイル: 左カラムの「カラーバリエーション対応シェーダー」と同様の、下線の入ったスタイルを適用します。
> - 解説文の内容: 現時点ではプレースホルダーを配置するか、特定の文章があれば差し替えます。（※今回は指示に従い、欄を作成します）

## 提案される変更点

### 作品紹介コンポーネント

#### [MODIFY] [index.html](file:///g:/Git/Portfolio/index.html)
- **右カラム（評価とツール）の調整**:
    - 画像グリッド (`image-grid-2x2`) の直前に、`<h4 ...>ギャラリー</h4>` を挿入。
    - 画像グリッドの直後に、背景に馴染む解説文用の `div` を追加。
    - 解説文エリアには、他のカードと統一感のあるテキストスタイル（`color: #ccc`, `font-size: 0.95rem` 等）を適用します。

## 実施計画

1. `index.html` の 2011行目付近にタイトルを挿入。
2. 2017行目付近（グリッド終了後）にテキストエリアを挿入。
3. 全体のバランス（余白）を確認。

## 検証計画
- タイトルが正しく表示されているか。
- 画像の下に期待通り文章入力用のエリアが作成されているか。
- レイアウト崩れがないか確認。
