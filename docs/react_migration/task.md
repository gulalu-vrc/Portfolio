# React 再構築 タスクリスト

- `[/]` 既存ファイルのバックアップ (`legacy/` へ退避)
- `[ ]` Vite + React プロジェクトの生成と依存関係インストール
- `[ ]` 画像リソースを `public/` へ移行
- `[ ]` UI機能用ライブラリ (`lucide-react` 等) の導入
- `[ ]` `style.css` のReact (`index.css`) への移植とグローバルスタイル設定
- `[ ]` 基本レイアウトコンポーネント (`App.tsx`, `Sidebar.tsx`) の作成
- `[ ]` `HeroSection` コンポーネントの作成
- `[ ]` 汎用的な `WorkSection` (作品ページ) コンポーネント群の作成
- `[ ]` 各作品コンテンツ (PreciousHarmony, 大手前大学OC 等) の流し込み
- `[ ]` `script.js` のスクロール監視 (ScrollSpy, FadeIn) 処理をReact Hooks (useEffect/IntersectionObserver) 化
- `[ ]` ローカルサーバー起動・動作検証・レスポンシブ崩れチェック
